<Window x:Class="ALauncher.View.LauncherMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ALauncher.View"
        mc:Ignorable="d"
        Width="500"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Name="TxtBlck" Grid.Column="1" TextWrapping="Wrap" Style="{StaticResource LauncherTextBlockStyle}" Text="Hello world!"/>
        <Image Name="IconImage"/>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2">
            <Button Name="OkBtn" Width="80" Style="{StaticResource GreenButtonStyle}" Margin="10" Content="Ок"/>
        </StackPanel>
    </Grid>
</Window>
